# ChordFlow 产品需求文档

> 版本：0.24
> 更新日期：2026-09-22
> 状态：v0.29.0 贯通常用九和弦输入、声部、Riff 与交付

## 1. 产品定义

ChordFlow 是一个结构驱动、语料增强的音乐和弦生成器。系统先理解用户需要的曲式、调性、风格、情绪与熟悉度，再为主歌、副歌、桥段和尾奏生成彼此连贯的和弦方案，并提供段落过渡、加花、试听、解释与导出。

长期目标是建立经过许可审查、去重和统一归一化的百万级歌曲和弦图谱，以真实语料概率、音乐理论规则和约束搜索共同驱动生成。项目不应把当前的小规模语料统计描述为“全球使用率”，也不应把百万级目标描述成已经完成的能力。

## 2. 核心价值

1. **符合音乐规律**：结果满足调式、和声功能、终止、张力解决、段落能量和基本声部连接要求，能够匹配用户设定的听感与调性。
2. **降低雷同性**：利用可追溯语料统计识别高频路径，通过熟悉度—惊喜度控制、俗套惩罚和低频替代方案降低机械重复。
3. **完成和弦设计**：不仅生成四和弦循环，还要处理主歌与副歌之间的连续性、重复段落变体、经过和弦、转位、次属和弦、借用和弦和终止式加工。
4. **衔接音乐生产工具**：输出可复制的 Suno 制作提示词、结构化和弦说明、MIDI 与 JSON，缩短从和声构思到音乐生成的路径。

## 3. 目标用户与使用场景

### 3.1 目标用户

- 不熟悉乐理、但能用听感描述目标的音乐爱好者。
- 需要快速寻找主歌、副歌和桥段和声骨架的词曲作者。
- 使用 Suno 等生成式音乐工具，需要更具体音乐控制信息的创作者。
- 需要语料依据、可解释替代项和 MIDI 草稿的制作人。

### 3.2 典型任务

- “为一首苦甜、克制的华语流行歌曲生成 ABABCB，C 大调，副歌更有释放感。”
- “保留主歌前三个和弦，只降低副歌与常见 1645 的雷同性。”
- “为桥段回到最终副歌设计三种复杂度不同的过渡。”
- “把整曲和弦、结构、速度、乐器与制作方向整理成可复制到 Suno 的提示词。”

## 4. 输入要求

系统应支持以下创作条件：

- 曲式：ABA、AABA、ABAB、ABABCB、ABCBA、ABACA 与自定义结构。
- 段落：前奏、主歌、预副歌、副歌、后副歌、桥段、间奏、独奏、尾奏。
- 调性：12 个主音，大调、小调与后续扩展调式。
- 风格：华语流行、独立流行、R&B、摇滚、民谣、爵士流行、电子、电影配乐等。
- 情绪与听感：明亮、温暖、苦甜、怀旧、悬疑、梦幻、宏大、克制等多标签。
- 速度、拍号、段落长度与和声节奏。
- 和声复杂度、熟悉度—惊喜度、目标俗套风险。
- 必须保留的和弦、段落、低音或终止方式。
- 目标下游：Suno 提示词、MIDI 草稿、和弦谱或结构化工程数据。

## 5. 功能需求

### FR-01 曲式与段落规划

- 用户先选择曲式，再生成段落和弦。
- 相同字母段落必须保持主题身份，同时允许结尾、转位、节奏与色彩变化。
- 主歌、副歌和桥段应具有可解释的能量与张力差异。
- 支持插入前奏、预副歌、后副歌、间奏和尾奏。

### FR-02 音乐规律约束

- 生成结果必须符合用户设定的调性与调式，临时离调需要说明来源与解决目标。
- 强张力和弦必须解决，或明确标记为开放结尾。
- 对共同音、外声部运动、低音跳进、转位和音区进行评分。
- 和声节奏应与拍号、速度和段落角色匹配。
- 所有生成结果必须可用固定随机种子复现。

### FR-03 语料和和弦图谱

- 建立跨数据源的统一和弦、调性、段落与元数据模型。
- 先按歌曲去重，再计算和弦事件、歌曲覆盖、段落覆盖和条件转移概率。
- 支持按风格、年代、地域、段落和调式切片。
- 每项统计必须显示来源、样本量、统计口径、版本与许可证状态。
- 百万级歌曲是长期目标；在达到前必须在产品中展示实际有效样本量。
- 不分发原始受版权保护的音频、歌词或未经允许的完整歌曲级标注。

### FR-04 降低雷同性

- 为每条走向计算语料内歌曲覆盖率、重复频率、跨风格泛化程度和 Surprise。
- 将“熟悉”和“俗套”分开：高频不等于质量低，但需要提供风险说明。
- 每个高频走向至少提供三个功能合理、相对低频的替代方案。
- 用户提高惊喜度时，系统应提升色彩与罕见度，但不能破坏调式、锁定内容和解决逻辑。
- 支持对两个候选方案进行 A/B 试听和差异解释。

### FR-05 和弦设计与段落过渡

- 自动分析所有段落边界，而不只处理当前选中的一处。
- 支持原样直达、转位低音、共同和弦、次属和弦、减七经过、ii–V、调式借用、backdoor、属链与延迟解决。
- 提供至少三个复杂度等级，并显示张力变化、增加的和弦与适用风格。
- 用户可单独试听、接受、拒绝、撤销或恢复每个加工方案。

### FR-06 可解释生成

- 每个和弦说明名称、罗马数字、功能、听感、常见度和推荐原因。
- 每个段落说明其和声目标、与前后段落的关系以及再现时做了什么变化。
- 每个低频建议需要说明“为什么仍然合理”，避免只用概率排名。
- 数据事实、音乐理论规则和主观听感标签必须在界面上区分。

### FR-07 试听、编辑与导出

- 支持单和弦、走向、段落、边界与整曲试听。
- 支持基础钢琴、电钢、合成器铺底和分解和弦伴奏型。
- 支持锁定、替换、局部重新生成、撤销/重做和版本对比。
- 导出 MIDI、JSON、纯文本和弦谱及可分享的项目文件。
- MIDI 应包含速度、拍号、标准段落 marker、和弦时值、转位与低音信息。

### FR-08 Suno 制作提示词

- 为整曲和每个段落生成中英文可复制提示词。
- 提示词至少包含：风格、情绪、速度、拍号、调性、曲式、段落能量、和弦走向、配器、演唱与制作方向。
- 同时输出“简短风格提示词”和“结构化制作说明”，避免把全部音乐信息塞进一个长句。
- 允许按段落锁定能量、声部路径与三档配器密度；能量和声部同步进入提示词、试听、JSON 与 MIDI，配器密度同步进入提示词、TXT 与 JSON，但不映射为虚假的 MIDI 音轨控制。
- 三档配器密度必须结合当前风格输出具体乐器动作，并用紧凑的 `TEXTURE ARC` 汇总整曲配器起伏，避免通用词堆叠。
- 对风格提示和和弦蓝图执行轻量体检：使用明确标注的内部字符预算，并把高能量留白、低能量满配等组合列为“需确认”而非硬错误；不得宣称这些预算是 Suno 官方限制。
- 使用固定创作简报对 Style、TEXTURE ARC、逐段制作措辞、体检状态和完整导出产物进行快照回归；任何有意变更都必须显式更新基线。
- 提供一键复制与纯文本/JSON 导出。
- 如果未来接入 Suno API，只能使用官方允许的接口和授权流程。
- 必须提示：Suno 对和弦提示的执行具有模型不确定性，复制提示词不能保证逐小节严格复现；需要精确和声时应同时使用 MIDI 或在 DAW 中制作。

### FR-09 项目管理

- 保存用户输入、随机种子、语料版本、算法版本和全部编辑记录。
- 支持自动保存、项目复制、版本命名和恢复。
- 支持导入当前导出的 Arrangement JSON 与版本化本地工程 JSON，并在导入前完成结构校验和兼容迁移。
- 支持轻量 A/B 双快照、差异摘要、快速切换和撤销；默认仅保存在当前会话，不引入账户或数据库。
- 数据默认本地保存；云同步和协作属于后续能力。

### FR-10 和弦驱动 Riff（v0.19 已实现）

- 当前段落可输入 2–8 个数字级数、罗马数字或基础和弦名；小调数字按自然小调配和弦。
- v0.25：接受 V7/vi、vii°7/V 等明确次属目标，保留目标进入移调和 JSON；非法目标不能静默退化为其他和弦。斜杠低音仍通过低音锚点编辑，不与次属语法混用。
- 提供舒缓分解、切分律动、旋律钩子三个方向；1–2 小节动机循环适配当前和弦，优先使用和弦音。
- 分别调整节奏、音高、疏密、音域与句尾变化；主副歌使用同一动机，副歌抬高音区。
- 提供音符网格、独奏、合听和段落循环；保留动机参数参与移调、历史、保存和 JSON 恢复。
- 导出独立 Riff MIDI 轨、本段 WAV 合成参考音频，并在 Suno 蓝图中描述动机意图。MIDI 供精确编辑，音频供支持上传的工作流使用。
- v0.21：按 A/B/C 主题独立设置或静音，同字母再现继承；可恢复全曲默认。新主题编辑优先于全局修改，全局变更不覆盖独立主题。
- v0.21：经过音仅插入同一和弦内的弱拍，以一个或两个半音连接两端和弦音，不跨越换和弦边界。可选句尾落在最后和弦根音；不是强制回主和弦，也不保证主观更好听。
- v0.21：音符网格区分和弦音、经过音与收束音；主题设置进入 JSON、移调、撤销/重做和方案比较，静音段保留时间位置。旧工程默认不开启新修饰。
- v0.24：可选两小节问答，每组第一小节末尾保留一个主拍休止，第二小节沿用开头节奏与目标轮廓并适配当前和弦，答句末尾落在该小节末和弦根音；密集换和弦时延后落点以匹配末和弦。6/8 主拍按附点四分音符处理。
- 问答模式固定两小节与句尾策略，不覆盖原循环的句尾变化和落点设置；切回循环恢复这些设置。经过音不得填入问句休止，音符不越过和弦边界。音符网格标注问句/答句，配置与输出沿用现有主题、保存、移调、撤销、MIDI、WAV 和 Suno 通路。
- 休止约束作用于音符起止；实时合成器释放尾音仍可能延续。当前不生成跨段对答、不评估 Riff 好听概率。
- v0.25：可选弱拍预示，在八分音符网格上，提前半个四分音符奏出下一和弦实际旋律锚点，衔接跳进不超过两个半音；已有共同音不另标预示。保护问句留白和收束音，非整格边界或无实际落音时跳过。仅处理本段内部，不跨段修改独立动机；旧工程默认关闭。
- v0.25：实时 Riff 使用独立三角波、40 ms 释放尾音，和弦音源保持原设置。可连前后段试听 Riff，游标仅显示当前段内的局部拍位置；停止时取消两种音源的待播音符。
- WAV 可选和弦合奏或纯 Riff，文件名区分，均使用与 MIDI 相同的 Riff 音符源；三角波离线参考和实时音色仍有包络差异，不承诺波形相同或 Suno 精确跟随。
- v0.26：每种方向提供 3 个基础节奏/轮廓与 8 类规则变换，节奏保留小节首音、限制到当前八分音符网格；变换后的轮廓限制在中心上下 9 半音内，再沿用句尾变化与当前和弦映射。问答留白、句尾根音仍优先。
- 节奏和音高分别使用可选版本号；缺少版本号的任意合法旧种子继续按旧算法解释。只有点击对应按钮时才启用该维度的新变体，另一维度的种子和版本原样保留。
- 变体切换最多检查一轮 24 个配方，跳过当前段基础音符结果相同的候选。换节奏保留目标轮廓设置，换音高保留基础节奏；经过音和预示音按结果重新计算，因此不承诺修饰后所有起点不变。
- 变体名称和 Suno 制作意图必须对应保存的设置；归档、主题覆盖、撤销、移调与导出可复现。小节压缩、密度叠加和和弦映射可能让不同配方产生同一结果，不宣称 24 种唯一或更好听的旋律。
- v0.27：可选「段尾引入下一段」，在段尾最后一个八分音符重复下一段实际起拍音，仅接受同音或不超过两个半音的连接。只改本段末尾，不重写后段；问答与根音收束优先，曲尾、后段静音、末和弦无可用前音或跨度不合适时跳过，已有相同尾音不重复加工。旧工程默认关闭。
- 先按完整曲式生成 Riff，再截取和平移片段，网格、单段/连段试听、MIDI、WAV 共享计划；截取不重新推断后段。循环片段保留原后段引入音，不擅自变成首尾循环衔接。该一致性仅针对 Riff，和弦转位仍按现有片段逻辑规划。
- 衔接设置随主题、保存、JSON、撤销与移调恢复，音符不作为临时缓存写入工程；修改后段实时重算。界面区分实际生成与未加工，Suno 说明引用实际引入音，不把开关开启等同于成功加花。
- v0.28：重音方式可选原力度、主拍突出、弱拍推动。力度处理在音符与衔接生成之后，仅改变 velocity；音高、起点、时值、问答留白和衔接条件不变，不增加切分音。4/4、3/4 主拍为四分音符，6/8 为附点四分音符。
- 新重音沿用逐段能量：旋律在目标拍位加强，其他拍位减弱；经过、预示、段间引入音保持轻，句尾根音不压弱。旧工程缺省和显式原力度必须保持既有输出。主题独立/继承/静音、撤销、JSON、移调及片段交付沿用现有机制。
- 网格以亮度表现力度并显示 MIDI 0–127 数值，实际播放、MIDI、WAV 共用力度；Suno 只表达制作方向，不保证严格跟随。力度值不等于感知响度或好听概率，切换重音不构成等响度盲听。
- v0.29：接受 maj9、m9、9 和弦名，以及大写级数 maj9、大小写级数 9 与 V9/目标；大九、小九、属九必须分别保留大七/小七音和九音。add9 仍是三和弦加九音，不含七音。数字简写与现有自动模板不自动升级。
- 九和弦以五个和弦音进入声部规划，Riff 从完整和弦音集中选择；不强制每条 Riff 都突出九音。九音可作为手动低音锚点，标为第四转位。移调、JSON、试听、MIDI、WAV 与 Suno 保留性质，现有 add9 快速试听纠正为九音位于上方的原位排列。
- 上下文搜索识别 V9 和 V9/目标的解决方向，也接受后段九和弦为目标；不扩大候选枚举或伪造九和弦单独语料统计。7b9、13、小大九等未实现输入必须拒绝，不能当普通三和弦静默接受。

### FR-11 听觉辅助和弦挖掘（v0.19 原型）

- 受调性约束搜索四和弦候选，给出三个探索方向并允许试听、采用。
- 分别显示声部连接、假定音色粗糙度、语料转移覆盖和模板差异，不输出未经验证的“好听概率”或“原创率”。
- 次属转移、未见转移与无同长度模板明确处理为缺少相应证据。
- v0.22：候选中的次属/导音目标必须立即匹配后续和弦的根音与和弦音，可跨本段末尾接向下一段；前段的明确属目标约束本段起点。末段自动候选收束到主和弦，手动编辑不受这些搜索约束限制。
- v0.22：用前后段声部移动参与排序，显示真实目标连接；可连同相邻段落试听。模板比较保留次属目标，不将 V7/vi 当成普通 V。
- 当前仅为规则与语料辅助搜索，未训练审美模型；后续通过同条件 A/B 偏好与盲听验证改进排序。依据见 [研究说明](HARMONY_RESEARCH.md)。

### FR-12 A/B 盲听（v0.23 增加模板对照）

- 默认比较一组挖掘结果和一组内置模板，也可选择两个挖掘候选互选；随机分配 A/B，选择前不显示对应来源、名称、和弦或指标。
- 对照模板须同调式、四和弦、编辑熟悉度不低于 75 且含明确主三和弦；保留原循环，仅移位到主和弦结尾。循环去重，优先匹配风格，排除循环相同的挖掘结果。当前大、小调各两组家族，是小型编辑基线，不是全球频率证据。
- 相同音色、速度、拍号、两小节长度、流动转位和 60 能量；移除 Riff、手动低音和逐段制作覆盖，按发声音数补偿基础力度。补偿不等同于感知响度或 LUFS 匹配。
- 完整听完两边才能选择；中断、切换或音频失败不计作完整试听。支持偏好 A/B、差不多、都不喜欢，允许揭示后采用任一候选。
- 每轮最多一条记录；本地保留最近 200 次，支持 JSON 导出，保留实际试听方案、指标、顺序、算法版本和时间。
- 模板对照分别统计偏好挖掘、偏好模板、差不多、都不喜欢及和弦对数；统计和弦对时合并移调、循环移位和 A/B 换位。重复试听保留次数，但不是独立样本，不输出未经验证的胜率或审美规律。
- 不自动训练；受约束随机基线、实际收益验证和偏好学习尚未实现。
- 先构造统一条件的独立片段，再生成候选，避免截断跨段解决。候选互选沿用算法 0.22，模板对照使用 0.23 并记录实验类型、来源和模板 ID；继续读取 0.20/0.22 记录，但不混入模板对照统计。
- v0.25：完整听完两边后、揭示前可选填主要判断依据：和弦连接、和声色彩、熟悉程度、张力与释放。随选择保存在本地并导出；留空合法，旧记录保持可读，理由不改变计票、不视为因果标签或训练完成。

## 6. 输出定义

一次完整生成至少包含：

1. 歌曲概览：曲式、调性、速度、拍号、风格、情绪与总长度。
2. 段落蓝图：每段角色、长度、能量、和弦、罗马数字与变化说明。
3. 边界设计：原始边界、加工方案、张力与解决目标。
4. 统计说明：语料覆盖、下一和弦概率、俗套风险及来源。
5. 试听与 MIDI。
6. Suno 提示词包：全曲提示、分段提示和结构化制作说明。

## 7. 质量要求与验收指标

- **理论有效性**：自动理论测试中的非法调式拼写、无目标次属和弦和未说明离调为 0。
- **段落连续性**：相同字母段落保留可识别骨架，不同字母段落达到设定能量差。
- **多样性**：相同输入在不同随机种子下产生可辨别方案，同时保持同一风格与目标。
- **可追溯性**：界面中的每个百分比都能追溯到数据源与统计版本。
- **可复现性**：相同输入、种子、数据版本和算法版本得到相同结果。
- **交互性能**：本地候选和局部替换目标反馈时间低于 100 ms。
- **导出一致性**：界面、JSON、MIDI、和弦谱与 Suno 说明中的曲式和和弦保持一致。
- **用户听感评测**：建立盲测集，分别评价合理性、段落反差、特色和可用性，不能只依赖理论测试。

## 8. 当前状态与目标差距

| 能力 | 当前状态 | 目标状态 |
|---|---|---|
| 和弦语料 | POP909：909 首华语流行、100,233 个折叠和弦事件 | 合法、去重、可切片的跨风格大规模和弦图谱，长期目标百万级歌曲 |
| 曲式语料 | Harmonix Set：912 首西方流行、9,675 个段落 | 与和弦事件对齐的多风格段落级统计 |
| 生成模型 | 13 类走向原型、规则与语料转移候选 | n-gram/学习排序 + 功能规则 + Beam Search + 声部连接评分 |
| 曲式生成 | 6 类预设、自定义结构、重复段落变体与统一段落小节数 | 可逐段编辑长度、能量曲线与全曲边界规划 |
| 加花过渡 | 4 类当前边界建议 | 全曲边界扫描、更多技法、复杂度控制、撤销与版本比较 |
| 试听 | Tone.js 和弦与整曲播放，支持三档转位、手动低音锚点和可回归播放调度 | 多音色、伴奏型与节奏模板 |
| 编辑能力 | 和弦替换、调性切换、主题锁定、同字母局部生成、48 步撤销/重做、单槽浏览器本地保存、JSON 工程导入与会话内 A/B 双快照；真实 Chrome 回归覆盖 A/B 切换、撤销/重做、保存与 JSON 导入 | 拖放、命名版本与更多候选并列比较 |
| Suno 工作流 | Suno Bridge：中英文提示、和弦蓝图、逐段制作锁定、风格敏感配器、TEXTURE ARC、提示词体检、8 类风格固定简报矩阵、TXT 与双轨 MIDI；真实 Chrome 下载与播放调度回归覆盖共享制作参数、手动低音锚点、多段覆盖和 MIDI 段落 marker | 小规模人工可用性验证与更多异常路径 E2E |
| 质量评估 | 123 项单元/领域测试，其中 8 项固定提示词回归；覆盖动机变体、问答、衔接、重音、九和弦与片段交付，Chrome 验证真实文件、撤销保存、连段游标、静音联动、力度与五音和弦输出 | 基于实际试听记录验证改进，再评估偏好排序 |

## 9. 改进优先级

### P0：从可演示升级为可信创作工具

1. 接入 ChoCo 等可许可数据，建立许可证矩阵、歌曲去重、统一和弦归一化和分段统计。
2. 把当前模板抽样升级为受约束的候选搜索，引入声部连接、和声节奏和段落目标评分。
3. BPM、拍号、小节数、声部、能量、配器密度、手动低音锚点与多段制作覆盖已建立真实下载和试听调度回归；继续覆盖更多调式及边界加工后的导出。
4. 建立固定输入的生成回归集与人工听感评分流程。
5. Suno Bridge 交付与本地项目核心编辑链路的浏览器回归已完成；继续覆盖本地版本恢复、错误导入与快捷键，不扩展为云端项目系统。

### P1：提高音乐制作可用性

1. 增加扩展音、伴奏型与更细的和声节奏编辑。
2. 支持风格、年代、地域和段落语料切片，并显示小样本警告。
3. 增加全曲边界扫描、最终副歌升调和多方案 A/B 对比。
4. 完善 MIDI 段落标记、和弦时值和制作说明导出。
5. 增加旋律音兼容检查，为用户输入旋律避开强冲突音。

### P2：扩展生态

1. 自然语言创作简报与参数自动解析。
2. 用户偏好学习、私人走向库和团队共享。
3. 在许可允许时接入更多在线统计或官方音乐生成 API。
4. 建立匿名、可退出的用户反馈闭环，用真实选择数据校准听感标签与推荐排序。

## 10. 数据规模说明

“百万首歌曲和弦图谱”是产品规模目标，不是当前仓库已经完成的数据事实。仅依赖公开开放数据很难自然达到百万级且保持歌曲级合法标注，因此需要组合以下路径：

- 使用许可明确的开放和弦数据集。
- 使用允许聚合统计的官方 API 或商业授权数据。
- 接收用户主动导入且有权使用的 MIDI、MusicXML 或和弦谱。
- 只保存和发布允许再分发的派生统计，不发布受保护的原始内容。
- 为每个统计结果维护来源、许可证、版本、去重与数据完整性记录。

规模不能替代质量。曲式、调性、段落、和弦时值和转调边界的可靠程度，应当作为数据权重进入生成模型。
